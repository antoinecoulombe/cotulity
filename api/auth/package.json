{
  "name": "api-auth",
  "version": "1.0.0",
  "description": "Cotulity's authentication microservice api",
  "main": "./dist/server.js",
  "scripts": {
    "start": "npx ts-node ./src/server.ts",
    "dev": "cross-env TZ=UTC NODE_ENV=development tsnd --inspect=0.0.0.0:9225 --respawn src/server.ts",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo:all",
    "seed": "npx sequelize-cli db:seed:all",
    "migrate:reset": "npm run migrate:undo && npm run migrate && npm run seed",
    "test": "cross-env NODE_ENV=test jest --testTimeout=30000 --detectOpenHandles",
    "pretest": "  cross-env NODE_ENV=test npm run migrate:reset",
    "test:one": " cross-env NODE_ENV=test npm run migrate:reset && jest $TEST --testTimeout=30000 --detectOpenHandles",
    "test:noci": "cross-env NODE_ENV=test npm run migrate:reset && jest --testPathIgnorePatterns='.*(_noci_).*' --testTimeout=30000 --detectOpenHandles",
    "coverage": " cross-env NODE_ENV=test npm run migrate:reset && jest --testTimeout=30000 --collectCoverage --detectOpenHandles"
  },
  "author": "coulombe.antoine@hotmail.com",
  "license": "ISC",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/express": "^4.17.12",
    "@types/jest": "^26.0.24",
    "@types/mysql": "^2.15.18",
    "@types/node": "^15.12.4",
    "@types/supertest": "^2.0.11",
    "@types/validator": "^13.1.4",
    "faker": "^5.5.3",
    "jest": "^27.0.6",
    "jest-extended": "^2.0.0",
    "nodemon": "^2.0.7",
    "sequelize-cli": "^6.2.0",
    "supertest": "^6.1.4",
    "ts-jest": "^27.0.4",
    "ts-node-dev": "^1.1.8",
    "tslint": "^5.20.1",
    "typescript": "^3.9.10"
  },
  "dependencies": {
    "@babel/core": "^7.14.6",
    "@babel/node": "^7.14.7",
    "@babel/preset-env": "^7.14.7",
    "@babel/preset-typescript": "^7.14.5",
    "@babel/register": "^7.14.5",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "cron": "^1.8.2",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "formidable": "^1.2.2",
    "jwt-decode": "^3.1.2",
    "mariadb": "^2.5.4",
    "passport": "^0.4.1",
    "passport-jwt": "^4.0.0",
    "passport-local": "^1.0.0",
    "reflect-metadata": "^0.1.13",
    "sequelize": "^6.6.2"
  }
}
